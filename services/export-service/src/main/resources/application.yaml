spring:
  application:
    name: export-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/export_service}
    username: ${DB_USER:export}
    password: ${DB_PASSWORD:export}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
server:
  port: 8108
